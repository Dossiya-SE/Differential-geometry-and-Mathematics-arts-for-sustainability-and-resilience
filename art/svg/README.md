# Exact SVG sources

SVG sources encode mathematical objects or declared explanatory structure. They use the project design tokens, embed titles and descriptions, and link to provenance records. Generated SVG files live under `figures/generated/`; publication promotion requires review under `MSR-VIS-001`.
