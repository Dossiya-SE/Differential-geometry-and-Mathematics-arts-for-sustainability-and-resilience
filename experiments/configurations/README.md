# Experiment configurations

Configurations separated from a registry record must be immutable, schema-valid, and referenced by checksum. No application-specific configuration is admitted while the application decision remains `NOT_SELECTED`, except within a clearly labeled candidate-feasibility dossier governed by the selection protocol.
